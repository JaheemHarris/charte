import { CcaTooltipDirective } from './cca-tooltip.directive';

describe('CcaTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new CcaTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
