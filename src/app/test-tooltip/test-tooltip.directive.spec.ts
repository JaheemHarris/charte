import { TestTooltipDirective } from './test-tooltip.directive';

describe('TestTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new TestTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
